# 🔍 航班匹配問題診斷

## 問題症狀

查詢 BR805 時：
- ✅ TDX API 成功返回數據（1 flights fetched）
- ❌ Worker 無法匹配到航班號
- ❌ 顯示「無此航班」

## 問題原因

TDX API 返回了航班數據，但 Worker 的過濾邏輯無法匹配航班號。

可能的原因：
1. TDX API 使用的航班號欄位名稱與預期不同
2. 航班號正規化邏輯有問題（BR805 vs BR0805）
3. 數據結構不符合預期

---

## 🔧 診斷步驟

我已經添加了詳細的調試日誌來診斷問題。

### 步驟 1：重新部署 Worker

**請在 Cloudflare Dashboard 中更新代碼：**

1. 打開 `flight-api-tdx-worker.js` 文件
2. **全選** (Ctrl+A) 並 **複製** (Ctrl+C)
3. 前往 https://dash.cloudflare.com/
4. **Workers & Pages** → `flight-api-tdx`
5. 點擊 **Quick Edit**
6. **全選刪除** 舊代碼，**貼上** 新代碼
7. 點擊 **Save and Deploy**

### 步驟 2：等待 1-2 分鐘

讓更新生效。

### 步驟 3：重新測試 BR805

在瀏覽器中打開（按 Ctrl+Shift+R 強制刷新）：

```
https://flight-api-tdx.bayterter.workers.dev/?flight=BR805
```

### 步驟 4：查看詳細日誌

**在瀏覽器中按 F12，查看 Console 選項卡。**

現在應該會看到新的調試日誌：

```
🔍 [TDX API] Searching TPE for BR805...
📥 [TDX API] Response from TPE
   Status: 200
   Results: 1 flights fetched (pre-filter)
   
   👇 重點：查看這些新的調試訊息
   Sample record keys: FlightNumber, AirlineID, Terminal, ...
   Sample flight fields: { FlightNumber: "805", AirlineID: "BR", ... }
   Looking for normalized: "BR805"
   Checking flight: BR805 (raw: BR805)
```

---

## 📊 需要您提供的資訊

**請複製以下日誌內容並告訴我：**

1. **Sample record keys**（數據結構的所有欄位名稱）
2. **Sample flight fields**（航班號相關的欄位值）
3. **Looking for normalized**（我們要查找的航班號）
4. **Checking flight**（實際檢查的航班號）

**範例輸出**：
```
Sample record keys: UpdateTime, FlightNumber, AirlineID, Terminal, Gate, ...
Sample flight fields: {
  FlightNumber: "805",
  FlightNo: undefined,
  AirlineID: "BR",
  ScheduleDepartureTime: "2025-10-13T10:00:00",
  ScheduleArrivalTime: null
}
Looking for normalized: "BR805"
```

---

## 🎯 可能的解決方案

根據調試日誌，我會實施相應的修復：

### 情況 1：航班號和航空公司代碼分開存儲

```json
{
  "FlightNumber": "805",  // 只有數字
  "AirlineID": "BR"       // 航空公司代碼
}
```

**修復方案**：已實施合併邏輯
```javascript
// 產生合併候選：BR805、BR-805
if (airline && numeric) {
  baseList.push(`${airline}${numeric}`);
}
```

### 情況 2：航班號帶有前導零

```json
{
  "FlightNumber": "0805",
  "AirlineID": "BR"
}
```

**修復方案**：正規化時去除前導零
```javascript
const digits = m[2].replace(/^0+/, '') || '0';
```

### 情況 3：使用不同的欄位名稱

```json
{
  "FlightNO": "BR805",  // 不是 FlightNumber
  "AirlineID": "BR"
}
```

**修復方案**：添加該欄位名稱到檢查列表

---

## 🔄 完整的調試流程

### 1. 查看日誌輸出

部署後測試 BR805，複製所有調試日誌。

### 2. 分析數據結構

根據日誌判斷 TDX API 的數據格式。

### 3. 修復匹配邏輯

調整 `getRecordFlightCandidates` 或 `normalizeFlightNumber` 函數。

### 4. 移除調試日誌

修復確認後，移除多餘的調試日誌以提升性能。

---

## 🧪 測試其他航班

修復後，請測試多個航班確認：

```
BR805  ✅ 長榮航空
BR189  ✅ 長榮航空
CI001  ✅ 中華航空
TW669  ✅ 德威航空
JX801  ✅ 星宇航空
```

---

## 📝 常見的 TDX API 數據格式

根據 TDX 官方文檔，FIDS 數據可能有以下格式：

### 格式 A：分離存儲
```json
{
  "AirlineID": "BR",
  "FlightNumber": "805",
  "ScheduleDepartureTime": "2025-10-13T10:00:00"
}
```

### 格式 B：合併存儲
```json
{
  "FlightNumber": "BR805",
  "ScheduleDepartureTime": "2025-10-13T10:00:00"
}
```

### 格式 C：帶前導零
```json
{
  "AirlineID": "BR",
  "FlightNumber": "0805"
}
```

**我們的代碼已經處理了格式 A 和 B，以及去前導零邏輯。**

---

## 📞 下一步

**請執行以下操作：**

1. ✅ 重新部署 Worker（包含調試日誌）
2. ✅ 測試 BR805
3. ✅ 複製完整的調試日誌給我
4. ✅ 我會根據日誌修復問題

**部署後，請將完整的 Console 日誌貼給我，我會立即修復！** 🚀

