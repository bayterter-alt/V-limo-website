# 🔍 測試 TDX FIDS API 數據範圍

## 問題

用戶反映：選擇未來日期查詢時，只能看到今天或昨天的航班結果。

## 可能原因

### 1. TDX FIDS API 的數據特性

**FIDS (Flight Information Display System)** 是**機場航班資訊顯示系統**，主要用途：
- ✅ 顯示當天的實時航班狀態
- ✅ 提供即時的起降時間、登機門、航廈等資訊
- ❌ 不是用來查詢未來班表

### 2. TDX API 的不同端點

TDX 運輸資料平台可能有不同的 API：

| API 類型 | 用途 | 數據範圍 |
|---------|------|---------|
| **FIDS API** | 實時航班資訊 | 當天 ± 1天 |
| **Schedule API** | 航班班表 | 未來數週/數月 |
| **Historical API** | 歷史資料 | 過去數據 |

我們目前使用的是：
```
https://tdx.transportdata.tw/api/basic/v2/Air/FIDS/Airport/{airport}
```

這是 **FIDS API**，所以只有當天的數據！

## 🧪 驗證方法

### 測試 1：查看 API 返回的日期範圍

從之前的調試日誌中，我們看到：

```javascript
FIDSDeparture[0] sample: {
  FlightDate: '2025-10-12',  // 當天或前一天
  FlightDate: '2025-10-13',  // 當天
  ...
}
```

所有返回的航班都是當天或昨天的！

### 測試 2：檢查 TDX 文檔

根據 TDX 官方文檔：
- FIDS API 提供「機場當日航班動態資訊」
- 關鍵字：**當日**

## 🎯 解決方案

### 方案 1：接受 FIDS 的限制（推薦）

**調整前端提示和日期範圍**：
- 移除日期選擇器（因為 FIDS 只有當天）
- 或者保留但說明「僅供參考，實際以當天數據為準」
- 將日期範圍改為「昨天、今天、明天」（如果 FIDS 有提供）

### 方案 2：尋找 TDX Schedule API

如果 TDX 有提供航班班表 API，則：
- 查詢未來航班時使用 Schedule API
- 查詢當天航班時使用 FIDS API
- 需要研究 TDX 文檔找到正確的 endpoint

### 方案 3：混合數據源

- 當天：使用 FIDS（實時數據）
- 未來：降級到本地數據庫或提示用戶

## 📝 建議

### 立即方案（最實際）

由於 FIDS API 的特性，建議：

1. **移除日期選擇器**
   - FIDS 本來就是設計用於查詢「現在的航班狀態」
   - 日期選擇功能對 FIDS 沒有意義

2. **更新提示文字**
   ```
   「輸入航班號碼，查詢今日即時航班資訊」
   「本系統顯示當天航班的實時狀態」
   ```

3. **保持簡潔**
   - 只需航班號輸入框 + 查詢按鈕
   - 提供今日即時資訊就已經很有價值

### 長期方案（如果需要未來航班）

如果確實需要查詢未來航班：
1. 研究 TDX 是否有 Schedule API
2. 實施雙 API 策略（FIDS + Schedule）
3. 或使用其他航班班表數據源

## 🔗 參考資料

- TDX FIDS API 文檔
- 機場航班資訊顯示系統標準
- IATA/ICAO 航班資訊標準

---

## 結論

**FIDS API 設計上就是用於查詢當天航班，這是正常的行為。**

建議移除日期選擇器，專注於提供即時航班資訊的核心價值。


